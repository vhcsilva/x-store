import styles from "@/components/Footer/styles.module.css";

export default function Footer () {
  return (
    <footer className={styles.wrapper}>
      <span>HackLayer 3x - Team 3 &copy;</span>
    </footer>
  );
}